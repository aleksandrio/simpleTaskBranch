function findLongestWord(str) {
  arr = str.split(' ').map(function(val){
  return val.length;});
  arr = arr.sort();
  return arr.pop();
  }

findLongestWord("What if we try a super-long word such as otorhinolaryngology");
